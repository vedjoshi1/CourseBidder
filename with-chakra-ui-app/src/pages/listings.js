import Layout from "../components/MainLayout"

const Listings = () => {
    return (
        <Layout>
            <div>
                No current listings
            </div>
        </Layout>
        
    );


};

export default Listings;